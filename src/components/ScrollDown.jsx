/* eslint-disable react/prop-types */

const ScrollDown = () => {
	return (
		<div className="relative h-full flex items-center pointer-events-auto">
			<p className="tracking-[3px] uppercase cursor-default">SCROLL DOWN</p>
		</div>
	);
};

export default ScrollDown;
